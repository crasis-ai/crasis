# Specialist Scorecard

Results from completed training runs. All 10 specialists trained, evaluated, and exported.

---

## Trained

| Specialist | Task | Train | Eval | Synthetic Acc | Holdout Acc | Gap | ONNX Size | Latency | Base Model | Gate |
|---|---|---|---|---|---|---|---|---|---|---|
| invoice-intent | binary | 4,517 | 502 | 99.60% | **96.67%** | 2.9pp | 4.3 MB | 0.55 ms | BERT-Tiny (L2-H128) | PASSED |
| whatsapp-triage | binary | 2,746 | 306 | 98.04% | **96.67%** | 1.4pp | 4.3 MB | 0.56 ms | BERT-Tiny (L2-H128) | PASSED |
| availability-handler | binary | 3,519 | 392 | 98.72% | **93.33%** | 5.4pp | 4.3 MB | 0.62 ms | BERT-Tiny (L2-H128) | PASSED |
| pricing-detector | binary | 3,511 | 391 | 99.74% | **93.33%** | 6.4pp | 4.3 MB | 0.57 ms | BERT-Tiny (L2-H128) | PASSED |
| support-router | multiclass (5-class) | 4,317 | 480 | 96.25% | **84.00%** | 12.3pp | 4.3 MB | 0.57 ms | BERT-Tiny (L2-H128) | PASSED |
| social-classifier | binary | 2,691 | 299 | 99.67% | **86.67%** | 13.0pp | 4.3 MB | 0.56 ms | BERT-Tiny (L2-H128) | PASSED |
| meeting-parser | binary | 5,332 | 593 | 99.83% | **86.67%** | 13.2pp | 4.3 MB | 0.55 ms | BERT-Tiny (L2-H128) | PASSED |
| sentiment-gate | binary | 2,649 | 295 | 99.32% | **80.00%** | 19.3pp | 4.3 MB | 0.56 ms | BERT-Tiny (L2-H128) | PASSED |
| email-urgency* | multiclass (4-class) | 3,671 | 408 | 86.76% | **65.63%** | 21.1pp | 10.8 MB | 2.79 ms | BERT-Mini (L4-H256) | PASSED |
| spam-filter* | binary | 4,500 | 500 | 99.20% | **70.00%** | 29.2pp | 10.86 MB | 2.67 ms | BERT-Tiny (L2-H128) | PASSED |

Table sorted by holdout accuracy descending. **Holdout accuracy is the honest number** — measured on 25–50 hand-authored examples in `tests/fixtures/` that were not generated by the training pipeline.

---

## Notes

- **spam-filter and email-urgency are marked experimental** (`*`): holdout accuracy below 75% indicates significant synthetic-to-real distribution shift. Use these as starting points for hybrid training, not production classifiers.
- **ONNX sizes are post int8 quantization** — roughly 4x smaller than the float32 base model footprint.
  BERT-Tiny (17 MB float32) → ~4.3 MB quantized. BERT-Mini (43 MB float32) → ~10.8 MB quantized.
- **Latency** is median over 50 single-inference runs on CPU (single thread), no batching.
- **email-urgency accuracy is lower** by design — 4-class multiclass on a harder decision boundary (critical / high / normal / low). Gate was 85%, result was 86.8%.
- **support-router** is a 5-class multiclass trained on BERT-Tiny — tighter size budget, lower natural accuracy ceiling than email-urgency's BERT-Mini. 96.25% exceeds the 90% gate.
- **Training times** are on RTX 4060 (8 GB), 10 epochs, batch size 32, fp16.
- Data generation for binary tasks uses `deepseek/deepseek-chat-v3.1` (~20 min / 3,000 samples, 4 workers).
  Multiclass uses `meta-llama/llama-3.3-70b-instruct`.

---

## Accuracy Analysis

All accuracy figures are synthetic eval results. Initial holdout evaluation on hand-authored examples shows real-world accuracy lower for each model. Treat published figures as training convergence metrics, not field-validated performance claims.

**Synthetic accuracy** (the Eval column from training) reflects performance within the LLM-generated data distribution — not real-world text. All 10 specialists hit synthetic gates of 85–99%.

**Holdout accuracy** is measured by running the deployed ONNX model against hand-authored examples in `tests/fixtures/<name>.jsonl`. These examples were written without any generator templates, covering edge cases and phrasing patterns the synthetic pipeline tends to flatten out.

**The gap is the signal.** Large gaps indicate the model learned the generator's linguistic fingerprints more than the underlying task. Two specialists have small gaps (≤3pp) and can be considered field-ready. The rest have meaningful gaps that warrant more training data diversity, a hybrid strategy, or a larger base model.

| Gap range | Specialists | Interpretation |
|---|---|---|
| <5pp | whatsapp-triage, invoice-intent | Synthetic data transfers well; task boundary is crisp |
| 5–10pp | availability-handler, pricing-detector | Mild distribution shift; monitor in production |
| 10–20pp | support-router, social-classifier, meeting-parser, sentiment-gate | Meaningful gap; consider hybrid training data |
| >20pp | email-urgency, spam-filter | Significant gap; synthetic data alone is insufficient for these tasks |

To reproduce any holdout result:

```bash
crasis eval -s specialists/<name>/spec.yaml \
    -m ./models/<name>-onnx \
    --holdout tests/fixtures/<name>.jsonl
```
